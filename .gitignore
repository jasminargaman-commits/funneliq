# Secrets — never committed
.env
.env.*
*service_role*
*service_key*

# Data dumps — data lives in Supabase, not the repo (per project brief)
*.csv

# Trained models — small ones are committed for Railway to serve (see models/README / architecture
# decision); this still blocks any stray large .pkl accidentally dumped elsewhere in the repo.
*.pkl
!models/*.pkl
catboost_info/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
