# Secrets — never committed
.env
.env.*
*service_role*
*service_key*

# Data dumps — data lives in Supabase, not the repo (per project brief)
*.csv

# Trained models — offline artifacts, not source-controlled here (meta.json sidecars are kept)
*.pkl
catboost_info/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
